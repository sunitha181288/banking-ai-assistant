# frontend/.npmrc
# ──────────────────────────────────────────────
# save-exact: locks installed versions exactly
# so npm install never silently upgrades to a
# vulnerable version within a ^ or ~ range.
# ──────────────────────────────────────────────
save-exact=true
audit-level=high
